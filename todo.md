# 할 일

마지막 갱신: 2026-09-22

---

## 0. 다음 순서

정한 순서는 **A → C → D → B** 다. 근거는 각 항목에 적었다.
A 는 2026-09-22 에 끝냈다. **다음은 C(실수령액 입력 옵션)** 다.

MDIS 답변을 기다리는 중이지만 **A~D 는 답변과 무관하게 진행한다.**
답변이 걸리는 것은 광고를 붙이는 시점이지 개발이 아니다.

| | 할 일 | 상태 | 어디에 |
|---|---|---|---|
| ~~**A**~~ | ~~2순위 그래프 — 근속기간별~~ | **완료** | 아래 2번 |
| **C** | 실수령액 입력 옵션 | 바로 가능 | 아래 4번 |
| **D** | 계산기 3종을 다른 페이지로 분리 | 바로 가능 | 아래 6번 |
| **B** | 3순위 그래프 — 회사 규모별 혜택 보유율 | 바로 가능 | 아래 2번 |
| | 상위 N% 역산 · 직종 바꾸면 내 위치 · 성별 격차 | 바로 가능 | 아래 3번 |
| | MDIS 문의 (공개 게시 + **영리행위**) | **답변 대기** | 아래 4번. 2026-09-22 발송 |
| | `gh-pages` 정리 | 바로 가능 | 아래 5번 |
| | 애드센스 준비 | **막힘** | `dev-guide/todo.md`. 도메인 구매 + repo 이름 확정 + **MDIS 답변** 이 선행 |

왜 이 순서인가:

- **A 를 먼저** — `breakdowns` 구조를 방금 만들어서 지금이 가장 싸다.
  집계 하나 추가하고 컴포넌트만 만들면 된다. 데이터도 좋다(같은 40대인데
  근속 1년 미만 280만 / 20년 이상 480만, 1.7배). 회사 규모 그래프와 짝이 되어
  "어디서 일하나" + "얼마나 오래" 가 나란히 놓인다.
- **C 를 그다음** — 이건 그래프가 아니라 **정확도 문제**다. 세전이라고 안내는
  붙였지만, 사람들이 그걸 읽고 자기 세전 월급을 계산해서 넣을 거라 기대하는 건
  낙관적이다. 실수령액을 넣고 잘못된 순위를 보고 가는 사람이 계속 생긴다.
  미루면 계속 걸린다.
- **D 를 그다음** — 그래프를 다 붙인 뒤에 해야 페이지가 실제로 얼마나 긴지
  보고 판단할 수 있다. 먼저 하면 감으로 정하게 된다.
- **B 는 마지막** — 셋 중 가장 덜 급하다. 다만 임금 그래프가 아니라서
  다른 월급 사이트에 없는 각도이긴 하다.

---

## 1. 원자료 — 받아둔 상태 (2026-09-22)

`raw/` 에 2025년 8월 근로형태별 부가조사 원자료가 있다. **컬럼 27개**로 다시 받았고,
기존 9개에 아래가 추가됐다. 전부 임금 보유 행에 채워져 있는 것을 확인했다.

| 변수 | 쓸 곳 |
|---|---|
| `현재일관련사항_종사자규모코드` | 회사 규모 축 (1-4 / 5-9 / 10-29 / 30-99 / 100-299 / 300명이상) |
| `교육정도컨버젼코드` | 학력 축 (초졸이하~대학원졸 6구간) |
| `현재일관련사항_11차산업대분류코드` | 업종 축. A~U 21개라 8~9개로 묶어야 한다 |
| `근로시간형태코드` | **전일제 필터로 이미 적용함** (1 전일제 / 2 시간제) |
| `현재일관련사항_직장시작연월` + `조사연월` | 빼면 근속기간 |
| `현재일관련사항_고용계약기간정함여부` | 무기 18,837 / 유기 6,944 |
| 퇴직급여 · 상여금 · 시간외수당 · 유급휴일 혜택여부 | 축이 아니라 콘텐츠용 |
| `유연근무제_활용여부` | 활용 14.5% |
| `급여수령위치코드` | 파견 251 · 용역 706 |

쓸 수 없다고 판명난 것은 `scripts/README.md` 의 "임금과 같이 채워지지 않는 변수" 표에 적어 뒀다.

**`raw/` 는 2번 작업이 끝날 때까지 지우지 않는다.** 끝나면 즉시 파기한다
(통계법 제21조①3). `임시/` 의 zip 도 같이 지운다. 둘 다 `.gitignore` 로 막혀 있다.

### 적용 완료 — 전일제 필터

`filters.workTimeType: ["1"]` 로 시간제 1,277명을 뺐다. 집계 표본 17,170명.
중위값이 전체 317만 → 330만으로 올랐고, 시간제 비중이 큰 60대 이상이
250만 → 270만으로 가장 많이 움직였다. 30대는 변화 없음(330만).

---

## 2. 새 축을 그래프로 붙이기

### 설계 — `cells` 와 `breakdowns` 를 나눈다

JSON 에 `breakdowns` 블록을 따로 뒀다. 둘의 역할이 다르다.

| | `cells` | `breakdowns` |
|---|---|---|
| 답하는 질문 | "내 월급이 상위 몇 %?" | "집단별로 얼마나 차이 나지?" |
| 키 | 축 3개 교차 (`나이\|성별\|직종`) | 축 1~2개만 |
| 개수 | 178개. 축을 더하면 곱으로 는다 | 축을 더해도 덧셈으로만 는다 |
| 폴백 | 필요 | 불필요 (표본 미달 칸은 `null`) |

그래프는 축 한둘이면 충분한데 `cells` 에 축을 넣으면 조합이 터진다.
그래서 **그래프를 붙이면서도 폴백 재설계(가장 무거운 작업)를 미룰 수 있다.**
`Selection` 확장과 폴백 재설계는 "내 순위를 회사 규모별로도 본다" 를
하고 싶어질 때 그때 한다.

- [x] `scripts/lib/dimensions.mjs` 에 `COMPANY_SIZES` 추가
- [x] `build-salary-stats.mjs` 에 `buildBreakdowns()` 추가
- [x] `src/types/salary.ts` 에 `Grid` · `BreakdownCell` 추가
- [x] `CompanySizeChart.tsx` — 1순위 그래프 (나이대 × 회사 규모)
- [x] ~~**A. 2순위 — 근속기간별 (나이 고정)**~~ — 완료
- [ ] **B. 3순위 — 회사 규모별 혜택 보유율**

2·3순위는 `buildBreakdowns()` 에 집계를 추가하고 컴포넌트만 만들면 된다.
구조는 이미 있다.

### 그래프는 스위치 하나에 모은다 (2026-09-22)

모양이 같은 그래프를 세로로 쌓으면 중복처럼 보이고 스크롤만 길어진다.
그래서 `figure` 하나에 두고 **가로축만 갈아끼운다.**

`SalaryStats.tsx` 의 `gridTabs` 배열에 항목을 하나 넣으면 탭이 하나 는다.
항목에 `grid`(데이터) · `label`(스위치에 적힐 말) · `hint` · `focusedHint`
· `caption` 을 담는다. **B 를 붙일 때도 컴포넌트를 새로 만들지 않고
이 배열에 추가하면 된다.**

`AgeGridChart` 는 두 그래프가 같이 쓴다(원래 `CompanySizeChart` 였고
`git mv` 로 이름만 바꿨다). 그래프마다 다른 것은 문구뿐이라 전부 props 로 뺐다.

### 범례를 눌러 나이대를 들춰 본다

범례를 누르면 그 줄만 색이 가고 나머지는 회색이 된다. 같은 것을 다시 누르면
풀려서 다섯 줄이 모두 램프 색으로 돌아온다.

**이 선택은 위쪽 나이대 셀렉트와 따로 둔다.** 묶으면 그래프를 들춰 보는
것만으로 "내 월급 상위 몇 %" 결과까지 바뀐다. 위에서 나이대를 바꾸면
들춰 보던 것은 풀리고 그쪽을 따라간다(`useEffect`).

**강조할 때는 램프 단계를 쓰지 않는다.** 20대는 램프에서 가장 옅은 단계라
강조인데도 회색 줄들 사이에서 묻힌다. 한 줄만 색이 있을 때는 색으로 순서를
나타낼 일이 없으므로 나이대와 무관하게 `HIGHLIGHT_COLOR` 하나를 쓴다.
램프는 다섯 줄이 모두 보일 때만 쓴다.

### A. 근속기간 그래프 — 만든 결과

나이대별로 따로 그렸다. 20대가 10년 이상에서, 30대가 20년 이상에서
끊기는 것은 그 나이에 불가능해서이고 정상이다.

| | 1년미만 | 1~3년 | 3~5년 | 5~10년 | 10~20년 | 20년+ |
|---|---|---|---|---|---|---|
| 20대 | 266만 | 280만 | 291만 | 300만 | — | — |
| 30대 | 300만 | 300만 | 320만 | 350만 | 400만 | — |
| 40대 | 300만 | 300만 | 340만 | 374만 | 450만 | 500만 |
| 50대 | 300만 | 300만 | 300만 | 310만 | 359만 | 500만 |
| 60대+ | 249만 | 250만 | 250만 | 260만 | 300만 | 380만 |

**50대 줄이 눈에 띈다.** 중간 근속(3~10년)이 300~310만으로 40대보다 낮고,
20년 이상에서만 500만으로 튄다. 이직·재취업으로 들어온 사람들이라
조건이 다른 것으로 보이지만 **확인된 것은 아니다.**

### A. 근속기간 그래프 — 만들 때 주의했던 것

`현재일관련사항_직장시작연월` 과 `조사연월`(전부 `202508`)의 차이가 근속 개월수다.
구간은 1년 미만 / 1~3년 / 3~5년 / 5~10년 / 10~20년 / 20년 이상.

**나이를 고정하지 않으면 그래프가 거짓말을 한다.** 전체로 집계하면
1년 미만 276만 → 20년 이상 500만인데, 20년 근속자는 거의 다 50대라
근속 효과가 아니라 나이 효과를 보고 있는 것이다.

40대만 떼어 보면 나이 교란이 빠지고도 살아남는다(2026-09-22 실측):

| 근속 | 40대 중위 | 표본 |
|---|---|---|
| 1년 미만 | 280만 | 484 |
| 1~3년 | 300만 | 660 |
| 3~5년 | 320만 | 496 |
| 5~10년 | 350만 | 834 |
| 10~20년 | 420만 | 1,474 |
| 20년 이상 | 480만 | 606 |

그래도 **생존편향은 남는다.** 20년 버틴 사람은 애초에 조건이 좋은 회사일
가능성이 높다. "버티면 오른다" 로 읽히지 않게 문구를 조심할 것.

`ageByCompanySize` 와 같은 모양(rows = 나이대, cols = 근속구간)으로 만들면
`CompanySizeChart` 의 구조를 거의 그대로 쓸 수 있다.

### B. 혜택 그래프 — 만들 때 주의할 것

이건 임금 그래프가 아니라 **보유율(%) 그래프**라 `breakdowns` 의 다른 모양이
필요하다. `median` 대신 비율을 담는다.

회사 규모별 보유율(2026-09-22 실측, 전일제 상용):

| 규모 | 퇴직급여 | 상여금 | 시간외수당 | 유급휴가 | 유연근무 |
|---|---|---|---|---|---|
| 1-4명 | 95% | 80% | 40% | 70% | 3% |
| 5-9명 | 99% | 85% | 54% | 82% | 4% |
| 10-29명 | 99% | 87% | 66% | 89% | 18% |
| 30-99명 | 99% | 90% | 77% | 93% | 20% |
| 100-299명 | 100% | 90% | 80% | 95% | 26% |
| 300명+ | 100% | 95% | 87% | 98% | 37% |

퇴직급여는 어느 규모나 95% 이상이라 그래프에서 뺄 후보다. 차이가 큰
**시간외수당(40%→87%)** 과 **유연근무(3%→37%)** 가 볼거리다.

코드값이 항목마다 다르니 주의: 퇴직급여 `111`/`112`, 상여금 `21`/`22`,
시간외수당 `31`/`32`, 유급휴일 `41`/`42`, 유연근무 `1`/`2`.

### 그래프 색 규칙

나이대·회사 규모처럼 **순서가 있는 값은 색도 순서를 따라야 한다.**
서로 다른 색을 섞지 말고 한 가지 색의 명도 단계를 쓴다.
`CompanySizeChart` 의 `SERIES_COLORS` 가 그 예다 (파랑 250~700단계).
가장 옅은 단계는 배경과 2:1 이상 차이 나야 해서 250단계(`#86b6ef`)가 하한이다.

**주의: 축이 하나 늘면 조합 수가 몇 배가 된다.** 지금 178개인데 회사 규모
6구간을 더하면 1,000개를 넘는다. 폴백 순서를 다시 짜야 한다.
축을 한꺼번에 다 넣지 말고 회사 규모부터 하나씩.

다만 표본은 생각보다 여유 있다(2026-09-22 실측, 전일제 상용 17,186명 기준):

| 회사 규모 | 표본 | | 학력 | 표본 |
|---|---|---|---|---|
| 1-4명 | 1,723 | | 초졸이하 | 208 |
| 5-9명 | 2,388 | | 중졸 | 493 |
| 10-29명 | 3,829 | | 고졸 | 5,208 |
| 30-99명 | 3,921 | | 초대졸 | 2,841 |
| 100-299명 | 2,077 | | 대졸 | 7,224 |
| 300명이상 | 3,248 | | 대학원졸 | 1,212 |

나이대(13) × 회사규모(6) 77조합 중 표본 30 미만은 14개뿐이라
**회사 규모는 6구간을 묶지 않고 그대로 써도 된다.** 여기에 성별·직종까지
겹칠 때가 문제이고, 그건 폴백으로 푼다.

---

## 3. 지금 데이터로 바로 되는 것

원자료 없이 오늘 당장 만들 수 있는 것들. 화면이 복잡해지니 하나씩.

- [x] ~~**나이대 × 회사 규모**~~ — 완료. 같은 40대인데 1-4명 284만 / 300명+ 500만
- [ ] **상위 N% 되려면 얼마?** — 지금은 월급 → 순위인데 그 역방향.
      분위수 표를 뒤집기만 하면 된다.
      (30대 기준: 상위 25% 416만 / 상위 10% 550만 / 상위 1% 940만)
- [ ] **직종을 바꾸면 내 위치가 어떻게 되나** — 월급을 고정하고 직종별 순위 비교.
      같은 300만원이 단순노무에선 상위 35%, 전문가에선 상위 75%.
- [ ] **성별 격차** — 같은 나이대·직종에서 여성 중위 / 남성 중위.
      30대 기준 78~86%. 해석은 붙이지 말고 숫자만.

---

## 4. 확인이 필요한 것

- [x] ~~**임금이 세전인지 세후인지**~~ — **세전으로 확인됨 (2026-09-22)**.
      근로형태별 부가조사표 76번: "최근 3개월간 주된 직장(일)에서 받은
      **(세금 공제 전)** 월평균 임금 또는 보수". 작성 주의사항에는
      "**실수령액이 아닌** 세금 공제전 월평균 총 급여액(**상여금·봉사료 등 포함**),
      현물도 시가 환산 포함" 이라고 적혀 있다.
      → 화면에 세전 배지와 안내 문구를 넣었다.
- [ ] **C. 실수령액 입력 옵션** — 위가 확인됐으니 만들 수 있다. 사람들은 실수령액으로
      기억하는데 데이터는 세전이라, 그대로 넣으면 순위가 통째로 밀린다.
      세전 안내 문구만으로는 부족하다고 보는 이유는 0번에 적었다.

      만들 때: 입력칸 옆에 "세전 / 실수령액" 토글을 두고, 실수령액이면 세전으로
      환산해서 비교한다. 환산에는 4대보험료율과 근로소득 간이세액표가 필요하고
      **둘 다 해마다 바뀐다.** 상수를 한 곳에 모으고 기준 연도를 화면에 밝힐 것.
      부양가족 수에 따라 세액이 달라지므로 "대략" 이라고 분명히 적어야 한다.
- [ ] **MDIS 문의 — 답변 대기 중 (2026-09-22 발송)**

      민주가 문의를 올렸다. 두 가지를 물었다.

      1. **공개 게시 가능 여부** — 공공용 마이크로데이터로 산출한 집계 통계를
         공개 웹사이트에 게시해도 되는지. 원자료는 올리지 않고 분위수 값만
         올리며, 표본 30명 미만 조합은 넓은 기준으로 대체한다고 밝혔다.
      2. **영리행위 제한** — 아래의 새 쟁점.

      답변이 오면 여기와 `dev-guide/todo.md` 애드센스 항목에 반영한다.

### 새로 드러난 쟁점 — 광고를 붙일 수 있는가

문의를 쓰면서 알게 된 것이다. **통계정보 사이트 통합 이용약관 제15조 제2항**:

> 이용자는 당 사이트의 사전 승낙 없이 서비스를 이용하여 어떠한 **영리행위도
> 할 수 없습니다.** 단 개별 사이트에서 별도로 정하는 경우에는 예외로 합니다.

애드센스는 영리행위로 읽힐 수 있다. 쟁점은 두 개다.

- 단서의 "개별 사이트에서 별도로 정하는 경우" 에 **MDIS 이용자 준수사항이
  해당하는지**. 해당한다면 MDIS 공공용 자료 이용자에게는 이 제한이
  적용되지 않는다.
- 적용된다면 **"사전 승낙" 을 받는 절차가 따로 있는지**.

**이건 기능이 아니라 수익화 전체에 걸리는 문제다.** 이 프로젝트가 애드센스
1호 대상인데(`dev-guide/todo.md`), 답변이 부정적이면 대상 자체를 다시 정해야
한다. 개발(A~D)은 답변과 무관하게 진행해도 된다 — 광고를 붙이는 시점에만
걸리는 문제다.

---

## 5. 자잘한 정리거리

- [ ] `npm run deploy` 와 `gh-pages` 패키지 제거 — Actions 자동 배포로
      옮겼으니 쓸 일이 없다. `gh-pages` 브랜치도 정리 대상.
- [ ] `minSampleSize` 는 30 유지. 50 이상으로 올리면 관리자(표본 405명)가
      모든 조합에서 폴백돼 선택지가 죽는다.

---

## 6. 계산기 3종을 다른 페이지로 분리 (D)

### 정한 것 (2026-09-22)

메인 페이지는 **월급 데이터 사이트**로 순수화하고, 부자 · 저축 · 커피
계산기는 다른 페이지로 옮긴다. **삭제하지 않는다.**

```
index.html        → 월급 통계 + 그래프들  (광고를 붙일 곳)
calculators.html  → 부자 · 저축 · 커피
```

### 근거

- **이미 중복이다.** `C:\study\index.md` 기준으로 `WorkvivalKit` 이
  같은 계산기 모음(커피값 · 부자 · 저축 · 근무시간)의 React Native 판이다.
  여기서 빼는 건 기능을 잃는 게 아니라 중복을 정리하는 것이다.
- **애드센스.** 이 프로젝트를 1호 대상으로 정한 이유가 "실데이터 기반이라
  가치 없는 콘텐츠 반려를 피한다" 였는데, 단순 계산기가 붙어 있으면 그 강점이
  희석된다. **다만 이건 추론이고 심사 기준을 직접 확인한 것은 아니다.**
- **길이.** 그래프 셋이 붙으면 한 페이지에 다 쌓기엔 너무 길고 탭이 7개가 된다.

### 왜 라우터가 아니라 정적 페이지 2개인가

README 에 적혀 있듯 지금은 "라우팅을 쓰지 않고 한 페이지 + 해시라서
새로고침해도 404 가 나지 않는" 구조다. React Router 를 넣으면 GitHub Pages 에서
새로고침 404 가 생긴다. **Vite 멀티페이지는 그 문제가 없다.**
설정에 입력 파일 하나를 더하면 되고 기존 컴포넌트는 그대로 재사용된다.

### 잃는 것

월급 입력을 네 구역이 공유하던 구조(커밋 `458ad63`)가 의미를 잃는다.
다만 그 입력은 그래프 쪽에서 더 잘 쓰인다(기준선, 내 위치 표시).

---

## 갱신 주기

근로형태별 부가조사 원자료는 **매년 11월경** 새로 공개된다.
받아서 `npm run data:build` 다시 돌리고 JSON 만 교체하면 된다. 코드는
건드릴 일이 없다.

일정: https://mdis.mods.go.kr/mobile/ofrScheduleMainList.do
