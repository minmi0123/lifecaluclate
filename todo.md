# 할 일

마지막 갱신: 2026-09-20

---

## 1. 원자료 재다운로드 — 다음 작업

지금 데이터는 나이대 × 성별 × 직종 세 축뿐이다. 직장인이 더 궁금해하는 축이
같은 조사 안에 있는데 지난번에 안 담았다.

### 추가로 담을 변수

| 번호 | 변수명 | 볼 수 있게 되는 것 | 우선순위 |
|---|---|---|---|
| 25 | `현재일관련사항_종사자규모코드` | 대기업 vs 중소기업 | **1순위** |
| 53 | `교육정도컨버젼코드` | 학력별 | 2순위 |
| 24 | `현재일관련사항_11차산업대분류코드` | 업종별(우리 회사 업종) | 3순위 |

회사 규모가 제일 아쉽다. "대기업은 얼마 받나"는 직장인이 가장 많이 하는
질문인데 지금 답을 못 한다.

### 이미 담았던 변수 (그대로 다시 담을 것)

```
성별코드
현재일관련사항_8차직업대분류코드
현재일관련사항_종사상지위코드
만연령
연령계층코드
가중값
경제활동상태코드
급여형태코드
최근3개월간평균급여
```

`평소1주근로시간수` 는 **뺀다.** 근로시간은 비임금근로자에게만 묻고 임금은
임금근로자에게만 물어서, 둘이 같이 채워진 행이 하나도 없다. 전일제 필터로
쓸 수 없다.

### 다운로드 화면 설정 (지난번과 동일)

| 항목 | 값 |
|---|---|
| 통계항목명 | 노동 > 경제활동인구조사 > 8월_근로형태별(제공) > 2025 |
| 제목 | 나이대·성별·직종별 임금 분포 통계 산출 및 비영리 공개 웹사이트 게시 |
| 이용목적 | **기타** (`교육 및 학습용` 은 고르지 말 것 — 공표 시 목적 외 사용이 된다) |
| 데이터 형태 | **CSV(항목명포함)** |
| 읽기스크립트 | R, SPSS 체크 (코드북 역할) |

주소: https://mdis.mods.go.kr/dwnlSvc/ofrSurvSearch.do

### 받은 뒤

```bash
# 1. raw/ 에 두고 (git 제외됨)
# 2. scripts/mdis.config.json 의 columns 에 새 변수 한 줄씩 추가
# 3. 변환
npm run data:build
# 4. 출력의 코드값 분포를 R 스크립트와 대조
# 5. 확인 끝나면 raw/ 삭제 (통계법 제21조①3 — 활용 후 즉시 파기)
```

자료 형태·코드값은 `scripts/README.md` 에 정리해 뒀다.

---

## 2. 새 축을 화면에 붙이기

변수만 받아서는 안 되고 코드도 손봐야 한다.

- [ ] `scripts/lib/dimensions.mjs` 에 새 축 추가 (회사 규모 구간 정의)
- [ ] `scripts/build-salary-stats.mjs` 의 조합 생성 루프에 축 추가
- [ ] `src/types/salary.ts` 의 `Selection` 확장
- [ ] `src/lib/salaryStats.ts` 의 폴백 순서 재설계
- [ ] `SalaryStats.tsx` 에 선택 UI 추가

**주의: 축이 하나 늘면 조합 수가 몇 배가 된다.** 지금 178개인데 회사 규모
4구간을 더하면 700개를 넘는다. 표본이 얇은 칸이 급격히 늘어나므로 폴백
순서를 다시 짜야 한다. 축을 한꺼번에 다 넣지 말고 회사 규모부터 하나씩.

---

## 3. 지금 데이터로 바로 되는 것

원자료 없이 오늘 당장 만들 수 있는 것들. 화면이 복잡해지니 하나씩.

- [ ] **상위 N% 되려면 얼마?** — 지금은 월급 → 순위인데 그 역방향.
      분위수 표를 뒤집기만 하면 된다.
      (30대 기준: 상위 25% 416만 / 상위 10% 550만 / 상위 1% 940만)
- [ ] **직종을 바꾸면 내 위치가 어떻게 되나** — 월급을 고정하고 직종별 순위 비교.
      같은 300만원이 단순노무에선 상위 35%, 전문가에선 상위 75%.
- [ ] **성별 격차** — 같은 나이대·직종에서 여성 중위 / 남성 중위.
      30대 기준 78~86%. 해석은 붙이지 말고 숫자만.

---

## 4. 확인이 필요한 것

- [ ] **임금이 세전인지 세후인지** — 조사표에서 확인할 것.
      지금 화면에는 `최근 3개월간 평균 급여 · 상용근로자 기준` 이라고만 적혀
      있다. 세전이 확인돼야 "실수령액" 기능을 붙일 수 있고, 그게 직장인이
      가장 많이 찾는 것 중 하나다.
- [ ] **MDIS 에 공개 게시 가능 여부 문의** — 공공용 마이크로데이터로 산출한
      집계 통계를 비영리 공개 웹사이트에 게시해도 되는지. 답변을 받아두면
      근거로 남는다. 지금은 조문 해석에 기대고 있다.

---

## 5. 자잘한 정리거리

- [ ] `npm run deploy` 와 `gh-pages` 패키지 제거 — Actions 자동 배포로
      옮겼으니 쓸 일이 없다. `gh-pages` 브랜치도 정리 대상.
- [ ] `minSampleSize` 는 30 유지. 50 이상으로 올리면 관리자(표본 405명)가
      모든 조합에서 폴백돼 선택지가 죽는다.

---

## 갱신 주기

근로형태별 부가조사 원자료는 **매년 11월경** 새로 공개된다.
받아서 `npm run data:build` 다시 돌리고 JSON 만 교체하면 된다. 코드는
건드릴 일이 없다.

일정: https://mdis.mods.go.kr/mobile/ofrScheduleMainList.do
